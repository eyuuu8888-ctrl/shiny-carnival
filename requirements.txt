# LLM
openai>=1.30.0          # OpenAI / DeepSeek / 通义千问 / 任何兼容 OpenAI 协议的服务

# Config
pyyaml>=6.0
python-dotenv>=1.0.0

# Logging
loguru>=0.7.0

# WeChat adapter (Windows only, Python 3.9 - 3.13)
# wxauto / wxautox 目前都不支持 Python 3.14+，请用 Python 3.12
wxauto>=3.9.11; sys_platform == "win32" and python_version < "3.14"
