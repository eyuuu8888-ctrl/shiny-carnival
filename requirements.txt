# LLM
openai>=1.30.0          # OpenAI / DeepSeek / 通义千问 / 任何兼容 OpenAI 协议的服务

# Config
pyyaml>=6.0
python-dotenv>=1.0.0

# Logging
loguru>=0.7.0

# WeChat adapter (Windows only)
# 需要在 Windows 上手动安装: pip install wxauto
# 这里不强制依赖，避免 Mac/Linux 上安装失败
wxauto>=39.1.8; sys_platform == "win32"
