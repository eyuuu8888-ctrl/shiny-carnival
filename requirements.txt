# LLM: OpenAI / DeepSeek / Qwen / any OpenAI-compatible API
openai>=1.30.0

# Config
pyyaml>=6.0
python-dotenv>=1.0.0

# Logging
loguru>=0.7.0

# WeChat adapter (Windows only, Python 3.9 - 3.13)
# wxautox is the V2 rewrite of wxauto by the same author (cluic).
# If wxautox is unavailable, you can alternatively `pip install wxauto` manually.
wxautox>=39.1.0; sys_platform == "win32" and python_version < "3.14"
