# LLM: OpenAI / DeepSeek / Qwen / any OpenAI-compatible API
openai>=1.30.0

# Config
pyyaml>=6.0
python-dotenv>=1.0.0

# Logging
loguru>=0.7.0

# WeChat adapter (Windows only, Python 3.9 - 3.13)
# wxauto / wxautox do NOT support Python 3.14+ yet. Use Python 3.12.
wxauto>=3.9.11; sys_platform == "win32" and python_version < "3.14"
